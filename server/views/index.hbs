<!doctype html>
<html lang="ca">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>
  <link rel="stylesheet" href="/estils.css" />
</head>
<body>
  {{> header}}

  <main class="container">
    <h1>{{title}}</h1>
    <p class="subtitle">{{subtitle}}</p>

    <p class="notice">{{notice}}</p>

    {{#if showWarning}}
      <div class="warning">
        Avís: aquesta pàgina mostra una condició amb <strong>if</strong> (showWarning = true).
      </div>
    {{/if}}
  </main>
</body>
</html>
